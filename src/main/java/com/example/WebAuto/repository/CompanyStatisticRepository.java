package com.example.WebAuto.repository;

import com.example.WebAuto.entity.CompanyStatistic;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CompanyStatisticRepository extends JpaRepository<CompanyStatistic, Long> {
}
